═══════════════════════════════════════════════════════════════════════════════
  JWT ACCESS TOKEN & REFRESH TOKEN TEST GUIDE
═══════════════════════════════════════════════════════════════════════════════

## 📌 TOKEN FLOW

┌─────────────────────────────────────────────────────────────────────┐
│                      1. LOGIN                                        │
│  User: admin / password: 123456                                     │
│  ↓                                                                   │
│  Server response:                                                   │
│  - accessToken (15 minutes lifetime)                               │
│  - refreshToken (7 days lifetime)                                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  2. USE ACCESS TOKEN                                 │
│  Send accessToken in Authorization header                          │
│  to access protected resources                                      │
│  ↓                                                                   │
│  Token expires after 15 minutes                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                3. REFRESH ACCESS TOKEN                               │
│  When accessToken expires:                                          │
│  - Send refreshToken + userId                                      │
│  - Get new accessToken (15 minutes lifetime)                       │
│  - refreshToken still valid (7 days)                               │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  4. LOGOUT                                           │
│  Send userId                                                        │
│  Server invalidates refreshToken                                    │
│  User must login again                                              │
└─────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

## 🧪 TEST STEPS (PowerShell / cURL)

### STEP 1: LOGIN - Lấy Access Token + Refresh Token
─────────────────────────────────────────────────────────────────────────────

Command:
```powershell
$body = @{
    username = "admin"
    password = "123456"
} | ConvertTo-Json

curl -X POST http://localhost:3000/login `
  -H "Content-Type: application/json" `
  -d $body
```

Expected Response:
```json
{
  "message": "✅ Login successful",
  "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJhZG1pbiIsImxvZ2luVGltZSI6IjIwMjYtMDEtMTZUMDY6MDA6MDBaIiwiaWF0IjoxNzM3MDAwMDAwLCJleHAiOjE3MzcwMDA5MDB9.xxx",
  "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJhZG1pbiIsImxvZ2luVGltZSI6IjIwMjYtMDEtMTZUMDY6MDA6MDBaIiwiaWF0IjoxNzM3MDAwMDAwLCJleHAiOjE3MzcwMDYwMDB9.xxx",
  "expiresIn": "15m",
  "user": {
    "id": 1,
    "username": "admin"
  }
}
```

✅ **Save these tokens:**
- `accessToken` → dùng cho API calls
- `refreshToken` → dùng khi accessToken expire
- `userId` = 1 (for refresh later)

───────────────────────────────────────────────────────────────────────────────

### STEP 2: VERIFY ACCESS TOKEN - Kiểm tra token hợp lệ
─────────────────────────────────────────────────────────────────────────────

Command (Replace YOUR_ACCESS_TOKEN):
```powershell
$body = @{
    token = "YOUR_ACCESS_TOKEN"
} | ConvertTo-Json

curl -X POST http://localhost:3000/verify-token `
  -H "Content-Type: application/json" `
  -d $body
```

Expected Response (Valid Token):
```json
{
  "message": "✅ Access Token is valid",
  "data": {
    "id": 1,
    "username": "admin",
    "loginTime": "2026-01-16T06:00:00Z",
    "iat": 1737000000,
    "exp": 1737000900
  }
}
```

Expected Response (Expired Token):
```json
{
  "message": "❌ Invalid or expired token"
}
```

✅ **Kiểm tra:**
- `exp`: expiration time (15 phút sau login)
- Nếu token expire → move to STEP 3

───────────────────────────────────────────────────────────────────────────────

### STEP 3: REFRESH ACCESS TOKEN - Tạo token mới
─────────────────────────────────────────────────────────────────────────────

Command (Replace YOUR_REFRESH_TOKEN & USER_ID):
```powershell
$body = @{
    refreshToken = "YOUR_REFRESH_TOKEN"
    userId = 1
} | ConvertTo-Json

curl -X POST http://localhost:3000/refresh-token `
  -H "Content-Type: application/json" `
  -d $body
```

Expected Response:
```json
{
  "message": "✅ Access Token refreshed successfully",
  "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJhZG1pbiIsImxvZ2luVGltZSI6IjIwMjYtMDEtMTZUMDY6MDA6MDBaIiwiaWF0IjoxNzM3MDAwOTAwLCJleHAiOjE3MzcwMDE4MDB9.xxx",
  "expiresIn": "15m"
}
```

✅ **Bây giờ:**
- Dùng `accessToken` mới cho API calls
- `refreshToken` vẫn hợp lệ (7 ngày)
- Có thể refresh lại khi token mới expire

⚠️ **Error Case:**
```json
{
  "message": "❌ Invalid or expired refresh token"
}
```
Nếu refreshToken expire → phải login lại (STEP 1)

───────────────────────────────────────────────────────────────────────────────

### STEP 4: LOGOUT - Xóa refresh token
─────────────────────────────────────────────────────────────────────────────

Command (Replace USER_ID):
```powershell
$body = @{
    userId = 1
} | ConvertTo-Json

curl -X POST http://localhost:3000/logout `
  -H "Content-Type: application/json" `
  -d $body
```

Expected Response:
```json
{
  "message": "✅ Logged out successfully"
}
```

✅ **Sau logout:**
- accessToken vẫn hợp lệ (nếu chưa expire)
- refreshToken bị xóa khỏi server
- Không thể tạo access token mới
- Phải login lại

═══════════════════════════════════════════════════════════════════════════════

## 🔑 KEY DIFFERENCES: Access Token vs Refresh Token

┌────────────────────────────────────────────────────────────────────────────┐
│                      ACCESS TOKEN                                          │
├────────────────────────────────────────────────────────────────────────────┤
│ Lifetime:       15 minutes (short-lived)                                  │
│ Usage:          Attach to every API request                               │
│ Header:         Authorization: Bearer {accessToken}                       │
│ Risk:           If stolen, attacker has 15 min access                     │
│ Refresh:        Yes, use refreshToken to get new one                     │
│ Revoke:         Can't revoke (need blacklist for security)                │
│ Example:        Call /api/products, /api/orders, etc.                    │
└────────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────────┐
│                      REFRESH TOKEN                                         │
├────────────────────────────────────────────────────────────────────────────┤
│ Lifetime:       7 days (long-lived)                                       │
│ Usage:          Only when accessToken expires                             │
│ Header:         Send in request body (not header)                         │
│ Risk:           If stolen, attacker has 7 days access                     │
│ Storage:        Store in httpOnly cookie (secure)                         │
│ Revoke:         Can revoke (logout invalidates it)                        │
│ Example:        Call /refresh-token to get new accessToken               │
└────────────────────────────────────────────────────────────────────────────┘

BEST PRACTICE:
1. AccessToken in Authorization header (every request)
2. RefreshToken in httpOnly cookie or secure storage (never in localStorage)
3. If accessToken expires → use refreshToken to get new one
4. If refreshToken expires → user must login again

═══════════════════════════════════════════════════════════════════════════════

## 📊 COMPLETE TEST SEQUENCE

### Timeline:
┌─────────────────────────────────────────────────────────────────────────┐
│ T+0min:   1. LOGIN                                                       │
│           - Get accessToken (valid until T+15min)                        │
│           - Get refreshToken (valid until T+7days)                       │
├─────────────────────────────────────────────────────────────────────────┤
│ T+5min:   2. VERIFY TOKEN                                                │
│           - Call /verify-token with accessToken                         │
│           - Response: ✅ Valid (expires in 10 minutes)                  │
├─────────────────────────────────────────────────────────────────────────┤
│ T+16min:  3. VERIFY TOKEN (AGAIN)                                        │
│           - Call /verify-token with accessToken                         │
│           - Response: ❌ Expired!                                        │
├─────────────────────────────────────────────────────────────────────────┤
│ T+16min:  4. REFRESH TOKEN                                               │
│           - Call /refresh-token with refreshToken                       │
│           - Get NEW accessToken (valid until T+31min)                    │
├─────────────────────────────────────────────────────────────────────────┤
│ T+20min:  5. VERIFY TOKEN (NEW)                                          │
│           - Call /verify-token with NEW accessToken                     │
│           - Response: ✅ Valid (expires in 11 minutes)                  │
├─────────────────────────────────────────────────────────────────────────┤
│ T+25min:  6. LOGOUT                                                       │
│           - Call /logout with userId                                     │
│           - refreshToken deleted from server                             │
├─────────────────────────────────────────────────────────────────────────┤
│ T+31min:  7. TRY REFRESH (FAIL)                                           │
│           - Call /refresh-token with (now invalid) refreshToken         │
│           - Response: ❌ Invalid or expired refresh token                │
├─────────────────────────────────────────────────────────────────────────┤
│ T+31min:  8. LOGIN AGAIN                                                  │
│           - Call /login with credentials                                 │
│           - Get NEW tokens                                               │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

## 🛡️ SECURITY NOTES

1. **AccessToken Storage:**
   ❌ WRONG: localStorage (vulnerable to XSS)
   ✅ RIGHT: Memory + Secure HTTP header

2. **RefreshToken Storage:**
   ❌ WRONG: localStorage (vulnerable to XSS)
   ✅ RIGHT: httpOnly cookie (not accessible to JS)

3. **Token Transmission:**
   ✅ ALWAYS use HTTPS (not HTTP)
   ❌ Never send tokens in URL or logs

4. **Token Expiration:**
   ✅ Short accessToken (15 minutes) + Long refreshToken (7 days)
   ❌ Long-lived single token (risky if compromised)

5. **Revocation:**
   ✅ Logout invalidates refreshToken
   ❌ Can't revoke accessToken (use token blacklist)

═══════════════════════════════════════════════════════════════════════════════

## TEST CREDENTIALS

Username: admin    | Password: 123456
Username: user     | Password: password

═══════════════════════════════════════════════════════════════════════════════
